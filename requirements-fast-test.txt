# GPU-specific PyTorch installation is intentionally left to the CUDA platform.
# Install a compatible torch build first, then install these verification-pipeline packages.
transformers>=4.46,<5
peft>=0.13,<1
bitsandbytes>=0.46
accelerate>=0.34
Pillow>=10
